import { parsePuzzleInput } from "./puzzle2";

describe("Day 2 parsePuzzleInput function", () => {
  test("should successfully read input file and parse data", () => {
    const input = "test-input.txt";
    const output = parsePuzzleInput(input);
  });
});